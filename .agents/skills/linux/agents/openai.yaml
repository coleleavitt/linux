interface:
  display_name: "Linux"
  short_description: "Repo-specific patterns and workflows for linux"
  default_prompt: "Use the linux repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true